#!/bin/bash

cd "$(dirname $0)"
cd ..

npm_bin="$(npm bin)"

# in case during development or installed from git
if [ -d "src" ]; then
    $npm_bin/ts-node src/index.ts "$@"
else
    node dist/src/index.js "$@"
fi